query TransactionsSearch(
  $after: String
  $before: String
  $first: Int
  $filter: TransactionsFilter
  $sort: [TransactionsSortInput!]
) {
  transactions(
    after: $after
    before: $before
    first: $first
    filter: $filter
    sort: $sort
  ) {
    totalCount
    edges {
      cursor
      node {
        id
        blockHeight
        operationCount
      }
      __typename
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
